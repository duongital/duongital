---
title: 'Javascript'
description: 'Cheatsheet on Javascript language'
---

Count apperance of character in an array:

```javascript
let m = new Map();
for (const e of arr) {
	m.set(e, (m.get(e) || 0) + 1)
}
```
